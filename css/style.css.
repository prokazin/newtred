body {
    margin: 0;
    font-family: sans-serif;
    background: #0f0f0f;
    color: white;
}

#app {
    max-width: 500px;
    margin: auto;
    padding: 10px;
}

header h1 {
    text-align: center;
    font-size: 22px;
    margin-top: 10px;
}

nav {
    display: flex;
    margin-top: 10px;
}

nav button {
    flex: 1;
    padding: 12px;
    background: #222;
    border: none;
    color: white;
    font-size: 16px;
}

nav .active {
    background: #444;
}

.tab-content {
    display: none;
    margin-top: 20px;
}

.tab-content.active {
    display: block;
}

.balance {
    font-size: 20px;
    margin-bottom: 20px;
}

.crypto-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
}

.crypto-item {
    display: flex;
    justify-content: space-between;
    background: #1b1b1b;
    padding: 14px;
    border-radius: 8px;
}

.crypto-item button {
    background: #0088ff;
    color: white;
    border: none;
    padding: 8px 12px;
    border-radius: 6px;
}
