:root{
  --bg:#0f1724;
  --card:#0b1220;
  --accent:#00c2a8;
  --muted:#94a3b8;
  --danger:#ff5b5b;
  --glass: rgba(255,255,255,0.03);
}

*{box-sizing:border-box}
html,body,#app{height:100%}
body{
  margin:0;
  font-family:Inter, Roboto, Arial, sans-serif;
  background:linear-gradient(180deg,#071226 0%, #081426 60%);
  color:#e6eef6;
  -webkit-font-smoothing:antialiased;
}

header{
  display:flex;
  justify-content:space-between;
  align-items:center;
  padding:12px;
  border-bottom:1px solid rgba(255,255,255,0.03);
}
.brand{display:flex;gap:10px;align-items:center}
.logo{width:48px;height:48px;border-radius:8px;object-fit:cover}
.brand h1{margin:0;font-size:16px}
.slogan{margin:0;font-size:12px;color:var(--muted)}
.balance-box{font-weight:700;text-align:right;font-size:14px}

.tabs{
  display:flex;
  gap:6px;
  padding:10px;
  overflow-x:auto;
  border-bottom:1px solid rgba(255,255,255,0.03);
}
.tab-btn{
  background:transparent;
  border:1px solid rgba(255,255,255,0.03);
  padding:8px 12px;border-radius:8px;color:var(--muted);
}
.tab-btn.active{background:var(--accent);color:#022; font-weight:700}

main{padding:12px}
.tab{display:none}
.tab.active{display:block}

/* grid of coins */
.market-grid{
  display:grid;
  grid-template-columns: repeat(3, 1fr);
  gap:10px;
}
.coin-card{
  background:var(--card);
  padding:10px;border-radius:10px;box-shadow:0 2px 8px rgba(0,0,0,0.4);
}
.coin-header{display:flex;align-items:center;gap:8px}
.coin-header img{width:36px;height:36px;border-radius:6px;object-fit:cover}
.coin-name{font-weight:700}
.coin-price{font-size:16px}
.coin-change{margin-left:auto;font-weight:700}

.mini-chart{width:100%;height:80px}
.mini-controls{display:flex;justify-content:space-between;align-items:center;margin-top:8px}
.open-trade{background:var(--accent);border:none;padding:6px 10px;border-radius:8px;color:#022}

/* position panel */
.position-panel{margin-top:12px;padding:10px;background:var(--glass);border-radius:10px}
.position-panel h3{margin-top:0}

/* trade page */
.trade-container{display:flex;gap:10px;flex-direction:row}
.trade-left{flex:0 0 320px;background:var(--card);padding:12px;border-radius:10px}
.trade-right{flex:1;background:var(--card);padding:12px;border-radius:10px}
.trade-select label, .trade-params label{display:block;margin-bottom:8px}
input,select{width:100%;padding:8px;border-radius:6px;border:1px solid rgba(255,255,255,0.06);background:transparent;color:inherit}
.btn-primary{background:var(--accent);border:none;padding:8px 12px;border-radius:8px;color:#012;font-weight:700}
.btn-danger{background:var(--danger);border:none;padding:8px 12px;border-radius:8px;color:#fff}

/* history */
.history-list{display:flex;flex-direction:column;gap:8px}
.history-item{background:rgba(255,255,255,0.02);padding:8px;border-radius:8px;display:flex;justify-content:space-between;align-items:center}

/* ranking */
.ranking-list{display:flex;flex-direction:column;gap:8px}

/* responsive */
@media (max-width:900px){
  .market-grid{grid-template-columns: 1fr}
  .trade-container{flex-direction:column}
  .trade-left{width:auto}
}

footer{padding:8px;text-align:center;color:var(--muted);font-size:12px;margin-top:12px}
.note{color:var(--muted);font-size:13px}
